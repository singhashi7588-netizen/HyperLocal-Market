to create a react app: npm create vite@latest

17/09/2026:
    react
    Git--> initialization and pushing, cloning


    git:::

    for first time only---
    git init
    git remote add origin
    git branch -M main

    in other computer:

    git clone <repo-url>
-----------------------------------------------------------------
    to be done always::::

    git status- check all changed files
    git add
    git commit -m "Initialized the repo"
    git push origin main


    in other computer:

    git pull

------------------------------------------------------------------------
     
     for  branch:
        1. git branch - branch helps to show the current branch we are in 
        2. git fetch origin : it helps us to locally fetch all the branches created in the remote repo
        3. git branch -a : use to see all the  available branches
        4. git checkout (enter branch name) 
        5. 
    
    